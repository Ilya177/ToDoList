export const MAX_TODO_TITLE_LENGTH = 500;

export const PRIORITY_LABELS = {
  1: 'low',
  2: 'medium',
  3: 'high',
};

export const PRIORITY_VALUES = {
  low: 1,
  medium: 2,
  high: 3,
};

export const DEFAULT_PRIORITY_LABEL = PRIORITY_LABELS[2];

export const DEFAULT_PRIORITY_VALUE = PRIORITY_VALUES[DEFAULT_PRIORITY_LABEL]; 